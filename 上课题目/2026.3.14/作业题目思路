P3379
	LCA模板题
	f[u][j]表示u的2^j级祖先是谁
	为了求lca(u, v),首先把u,v调整到同一深度
		将深度差进行二进制拆分,把更深的点向上跳
	深度一样后,先判断是否u == v
	否则,利用倍增表f,二分向上跳
	
P5002
	如果u和v的lca是P,要满足什么条件？
	首先,u,v必定在以P为根的子树内
	如果u或v是P本身,那另一个节点可以在子树中任取
	否则 
		如果u和v属于P的同一棵子树,那lca(u,v)就不是P
		u和v只能属于P的不同子树
		计算子树的大小sz[v],其它所有子树的节点数之和
			就是sz[u] - 1 - sz[v]
			
P5836
	并查集的思路：相邻且类型一样的农场进行合并
		边建立在并查集的组长节点上
		缩点法 

	带权LCA思路： 
	在树上,从A到B的路径中,是否存在至少一个点
		它的权值是给定的数字x
	设p = lca(A, B),问题变为
		从A到p是否包含x
		从B到p是否包含x
	维护倍增表f的同时,维护另一个倍增表g
	g[u][j]表示从u到u的2^j级祖先这条路上,是否包含x
	
	用二进制的思路,把两种奶牛编码为0b01 和 0b10
	这样g[u][j]可以同时表示是否包含两种奶牛
	
P10113
	第一个要求：节点集合的LCA
		求a[1] a[2] ... a[m] 的LCA
		可以先求a[1]和a[2]的LCA,求出来是p
		在求p和a[3]的LCA,以此类推
	第二个要求：找编号最大的员工
		LCA的所有祖先都满足题目条件,找一个编号最大的
		dp[u]表示从根到u的最大编号
		dp[u] = max(u, dp[p]),p是u的父节点
		
P13016
	求数字x的最大因数,复杂度sqrt(x)
	x和它的最大因数y之间,相差一个x的最小质因数
	x在树里的深度就是x的质因数分解中,一共包含几个质数
	
	树最深也不超过31层,可以暴力求lca
	
P2420
	权重在边上
		g[u][j]表示从u走到u的2^j级祖先路过所有边的权值异或和
		然后做带权LCA问题
		
	dp[u]表示从u到根的异或和
		dp[u] = A[u] ^ dp[p]
		
	u到v的路径可以看作 根到u的路径 异或 根到v的路径
	
P3938
	颜色相同的点是同一轮出生的兔子
	每轮新出生的节点数量是斐波那契数列
	
	问题：在这棵树上,求节点u的父节点
	对斐波那契数列求前缀和,看u在哪个S[i]和S[i+1]之间
	如果u在第i轮是第v个生出来的兔子,那么u的父节点就是v
	
	树的深度不大,暴力求lca即可 
	 
P1967
	思路一：生成树+LCA 
		输入是一个图,限重较小的边尽量不用
		求图的最大生成树
		剩下的问题变成带权lca
		其中g[u][j]表示从u到u的2^j级祖先的路径中,最小的限重
		
		求的是从u到v路径上的最小限重,
			转换成求u到lca和从v到lca的最小限重
	
	思路二：二分+生成树+Tarjan离线化处理 
		求u到v的路径上的最小限重,让它尽可能大
		“最小值最大”问题
		二分限重mid,表示从u到v的路径限重不能低于mid
			把限重低于mid的边从图中删掉（处理时忽略这些边）
			判断u到v是否连通 