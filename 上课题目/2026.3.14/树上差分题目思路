树上差分
	A[u]表示树上节点u的权重 
	第一种：dp[u]表示u和u的父节点权重作差
	第二种：dp[u]表示u和u的所有直接子节点权重作差
	
P3128
	路径加：从u到v的路径上每个节点都加同一个数字
	类比一维数组上的差分：
		给A[u~v]加上同一个数字x
		相当于在差分数组dp[u] += x; dp[v+1] -= x;
	树上差分：
		u到v的路径，拆成两条
		u到lca，和v到lca
	如果用第一种差分，前缀和从根到u来求
		dp[lca]++; dp[u的下一个节点]--;
		u的下一个节点可能有很多，不好用
	如果用第二种差分，前缀和从叶子到u来求
		dp[u]++; dp[lca的下一个节点]--
		lca的下一个节点就是它的父节点
		dp[v]++; dp[lca]--;
		避免lca这个点加两次
	树上前缀和：	
	int dfs(int u, int p)
	{
		for(auto v : G[u]) if(v != p)
			ans[u] += dfs(v, u);
		return ans[u];
	}

https://csacademy.com/app/graph_editor/以
1 2
1 3
1 4
2 5
2 6
4 7
为例 

树上的顺序（把树展平为一维数组）
dfs序：对树进行先序遍历，依次经过的点的顺序	

	dfn = {1 2 5 6 3 4 7}	dfs序的长度就是节点数
	
	int tid, dfn[maxn], sz[maxn], P[maxn]; //tid称为时间戳
	void dfs(int u, int p)
	{
		dfn[++tid] = u;
		P[u] = tid;
		sz[u] = 1;
		for(auto v : G[u]) if(v != p) 
		{
			dfs(v, u);
			sz[u] += sz[v];
		}
	}
	
	dfs序的性质：在同一棵子树里的节点，
		在dfs序里面一定是相邻的
		
	所以，以u为根的子树，它包含的所有节点
	在dfn的下标范围是[P[u], P[u] + sz[u] - 1] 
	
	在dfs序上支持 单点加 子树加 这种更新
				和 单点查询 子树求和 这种查询
				
广义dfs序/扩展dfs序/括号序
	对树进行先序遍历，进和出都要打时间戳
	1 2 5 5 6 6 2 3 3 4 7 7 4 1  长度是2n
	int tid, in[maxn], out[maxn];
	void dfs(int u, int p)
	{
		in[u] = ++tid;
		for(auto v : G[u]) if(v != p)
			dfs(v, u);
		out[u] = ++tid;
	}
	u的子树对应的节点在括号序内一定是连续的
	in[u]和out[u]之间的节点都是u的子树节点
	
	括号序上 进行  单点加 子树加 这种更新
		单点查询 子树求和 这种查询
		可以在in和out上都更新，最后查询结果除以2
		
	求从u到v的路径，权值之和？
	分解成 从根到u的路径+从根到v的路径-从根到lca的路径
		-从根到lca父节点的路径
		
	怎样在括号序上求从根到u的路径和？
		在in[u]处保存v[u]，out[u]处保存-v[u]
		则前缀和A[1 ~ in[u]]就表示根到u的路径和 

	支持单点修改，求路径和（树状数组）
	支持子树修改，求路径和（线段树）
		P3178难点在于操作2
			子树加 这种更新要求
			从in[x]到out[x]这个区间内
				是in的地方要加上a，是out的地方要减去a
			线段树维护区间和，在子树上区间和一定是0
			把in作为1，out作为-1，区间和就变成了修改a的倍数 
		
	
欧拉序
	沿着边对树进行先序遍历，把边的终点作为顺序
	1 2 5 2 6 2 1 3 1 4 7 4 1 		欧拉序的长度是2n-1
	
	欧拉序中，u会出现几次？u的度数（除了根节点） 
	欧拉序：in[u]表示第一次出现u的时间戳 
		在in[u]和in[v]之间，一定出现过u和v的lca
		这个点是在区间in[u]到in[v]之间深度最低的点
		问题变成找区间最小值（ST表） 