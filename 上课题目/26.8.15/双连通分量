双连通分量

无向图中
如果从u到v存在2条不同的路径，
则称u和v是双连通的

一个节点集合内，任意两点都是双连通的
则称这个点集是双连通分量 

不同的路径的两个含义：
经过的节点不能完全相同：点双连通分量
经过的边不能完全相同：边双连通分量

点双连通分量：在这个点集中，
	对于任意的u,v 
	删除任意一个非u非v的节点，u和v仍然连通 

边双连通分量：在这个点集中，
	对于任意的u,v 
	删除任意一条边，u和v仍然连通

割点（割顶）
	删除这个点（和它连接的所有边）
	图的连通分量会变多
	
	在树中，非叶子节点都是割点
	点双 的内部 没有割点
		的边界上都是割点 
	
割边（桥） 
	删除这个边
	图的连通分量会变多
	
	树的所有边都是割边 
	边双中没有割边
	
求割点
dfn tid low
不需要栈
bool cut[maxn]; //cut[u]表示u是否为割点
int rt; 		//本次tarjan的根节点 

对于树边u-v 
如果v出发，不经过u就不能访问到u上面
那么u就是割点，条件是low[v] >= dfn[u]
但是本次tarjan的根节点除外，需要特判

割点特判根 

void tarjan(int u)
{
	dfn[u] = low[u] = ++tid;
	int cnt = 0; 	//统计u在dfs树中有几个子节点 
	for(auto v : G[u])
	{
		if(!dfn[v])
		{
			cnt++; 
			tarjan(v);
			low[u] = min(low[u], low[v]);
			if(low[v] >= dfn[u]) //从v出发，如果不经过u，就不能访问到u上面的点 
				cut[u] = 1;	
		}
		else low[u] = min(low[u], dfn[v]);
	}
	if(u == rt && cnt == 1) cut[u] = 0;
}

求点双
不同的点双可能共用同一个节点，这样的节点一定是割点
对于边u-v
如果从v出发，不经过u则不能访问到u的上面
那么此时栈中所有v和v后面的点（再加上u）构成一个点双
孤立点需要特判，它自己构成一个点双，这个判定要求建图时不能有自环 
点双特判孤立点 
dfn tid low
s tp 	求点双需要用到栈
vector<int> dcc;  int dc; 	dcc[i]表示第i个点双，dc表示点双的个数

void tarjan(int u)
{
	dfn[u] = low[u] = ++tid;
	s[++tp] = u; 
	for(auto v : G[u])
	{
		if(!dfn[v])
		{
			tarjan(v);
			low[u] = min(low[u], low[v]);
			if(low[v] >= dfn[u])
			{
				dc++;
				do
				{
					dcc[dc].push_back(s[tp]);
				}while(s[tp--] != v);
				dcc[dc].push_back(u);
			}
		}
		else low[u] = min(low[u], dfn[v]);
	}
	if(G[u].empty()) dcc[++dc].push_back(u); //孤立点特判 
}

求割边
因为需要用到点的编号，因此G的定义要做修改
对于一条边u-v
在这条边不存在的情况下，如果v能访问到的点不会到u和u以上
则u-v是一条割边
割边特判指向父节点的反向边  
struct Edge
{
	int v, id; 终点，边的编号 
};
vector<Edge> G[maxn];

void tarjan(int u, int p)
{
	dfn[u] = low[u] = ++tid;
	int cnt = 0;  //统计返回父节点的边有几条 
	for(auto e : G[u])
	{
		int v = e.v;
		cnt += v == p; 
		if(!dfn[v])
		{
			tarjan(v, u);
			low[u] = min(low[u], low[v]);
			if(low[v] > dfn[u]) cut[e.id] = 1; 
		}
		else if(v != p || cnt > 1) low[u] = min(low[u], dfn[v]);
	} 
}

求边双
以求割边的程序为基础，将 if(low[v] > dfn[u])内部，改为
	if(low[v] > dfn[u])
	{
		dc++;
		do
		{
			dcc[dc].push_back(s[tp]);
		}while(s[tp--] != v);
	}
在主函数中，tarjan结束后，栈里剩下的点构成最后一个边双

注意点双和边双的区别：
	点双中要有u（u作为割点，是点双的边界）
	边双中没有u（u被割边切割到另一个边双中）
	
	点双在tarjan结束后，不需要清空栈（栈中剩余的是割点，不再构成新的点双） 
	边双在tarjan结束后，需要清空栈（栈中剩余的部分是新的边双）
	
	不同的点双之间可能共用节点（一定是割点）
	不同的边双之间没有共用节点（被桥分隔开） 

P3225
	如果点双
		有1个割点，需要设置1个救援出口
			如果割点坍塌，则这个点双内的点可以逃到本点双的救援出口
			如果救援出口坍塌，则可以通过割点逃到其他点双里
		如果有>=2个割点，则不需要设置救援出口
			无论坍塌哪个割点，都可以通过其他割点逃到其他点双里
		没有割点，需要设置2个救援出口
			一个坍塌了，就可以去另一个 